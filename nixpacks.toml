[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node dist/server/index.js"

[variables]
PORT = "3000"

[build.env]
NODE_VERSION = "20"

[[phases.build.dependsOn]]
phases = ["install"]

[static]
publishPath = "./dist"
