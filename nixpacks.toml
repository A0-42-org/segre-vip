[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]
dependsOn = "install"

[start]
cmd = "node dist/server/index.js"

[variables]
PORT = "3000"

[build.env]
NODE_VERSION = "20"

[static]
publishPath = "./dist"
